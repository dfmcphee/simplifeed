<div class="item">
  <div class="media upload alert alert-default">
    <button type="button" class="close btn-remove-upload" data-action="/files/<%= file.id %>" aria-hidden="true">×</button>
    <a class="upload-image" href="#">
      <img class="media-object" src="<%= file.small %>" alt="Upload Thumbnail">
    </a>
    <hr/>
    <div class="media-body">
      <h4 class="media-heading">Caption</h4>
      <input type="hidden" name="uploads[<%= file.id %>][file.id]" value="<%= file.id %>" />
      <textarea name="uploads[<%= file.id %>][file.title]"><%= file.title %></textarea>
    </div>
  </div>
</div>
